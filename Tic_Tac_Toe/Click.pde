boolean twoPlayer, onePlayer, nomode = true;


void clickXO() {
  if (nomode == false){
  if (mouseX >=  width*0  && mouseX <= width*1/3 && mouseY >= height*2/8 && mouseY <= height*4/8){
    textDraw(X, Font, height, ink, CENTER, CENTER, width*0, width*1/3, height*2/8, height*2/8);
  }
    
    
}
}
